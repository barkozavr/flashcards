%h2= @deck.name
%table.table.table-striped
  %thead
    %tr
      %th{ scope:'col' }= t 'card.page.original'
      %th{ scope:'col' }= t 'card.page.translate'
      %th{ scope:'col' }= t 'card.page.review_date'
  %tbody
    - @deck.cards.each do |card|
      %tr
        %th= card.original_text
        %th= card.translated_text
        %th= card.review_date.strftime('%d-%m-%Y')
        %th= card.decorate.current_picture(:thumb)
        %td= link_to t('card.page.destroy'), card_path(card), method: :delete, data: { confirm: t('card.js.sure?') }
        %td= link_to t('card.page.edit'), edit_card_path(card)
